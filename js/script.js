const users = [{}]

const tableHTML = document.getElementById("user-table")
const formHTML = document.getElementById("user-form")

console.log(formHTML)
console.log(tableHTML)

formHTML.addEventListener("submit", (evt)=>{

})